#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @i: int to be added first
 * @j: int to be added to
 *
 * Return: n
 */

int add(int i, int j)
{
	int n;

	n = i + j;
	return (n);
}
